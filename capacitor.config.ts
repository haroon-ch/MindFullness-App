import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.learn2earn.fashionly',
  appName: 'Fasionly',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
